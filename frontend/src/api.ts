// frontend/src/api.ts

const API_BASE_URL: string = 'http://localhost:5000/api'; // O el puerto que use tu backend
// Importante: En producción, cambia 'localhost:5000' por la URL pública de tu backend desplegado en Hostinger.

export default API_BASE_URL;